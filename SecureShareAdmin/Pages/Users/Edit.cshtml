@page
@model Snsc.SecureShareAdmin.Pages.Users.EditModel
@{
    ViewData["Title"] = "Edit User";
}
<h1 class="page-title">Edit User Information</h1>

@if (!string.IsNullOrEmpty(Model.Message))
{
    <p class="message-ssi">@Model.Message</p>
}
@if (!string.IsNullOrEmpty(Model.ErrorMessage))
{
    <div class="alert alert-danger">@Model.ErrorMessage</div>
}

@if (Model.ExternalUser != null)
{
    <form method="post" asp-page-handler="Save">
        <input type="hidden" asp-for="UserName" />
        <dl class="row">
            <dt class="col-sm-3">User Name</dt>
            <dd class="col-sm-9">@Model.ExternalUser.UserName</dd>
            <dt class="col-sm-3">Email</dt>
            <dd class="col-sm-9">@Model.ExternalUser.Email</dd>
        </dl>
        <div class="row mb-2">
            <div class="col-md-4">
                <label class="form-label" asp-for="FirstName">First Name</label>
                <input class="form-control" asp-for="FirstName" />
            </div>
            <div class="col-md-4">
                <label class="form-label" asp-for="LastName">Last Name</label>
                <input class="form-control" asp-for="LastName" />
            </div>
            <div class="col-md-4">
                <label class="form-label" asp-for="UserAccess">User Access</label>
                <input class="form-control" asp-for="UserAccess" type="number" />
            </div>
        </div>

        <h2 class="h6 page-title mt-4">Current Zones</h2>
        @if (Model.CurrentZones.Count == 0)
        {
            <p class="text-muted">No zones assigned</p>
        }
        else
        {
            <table class="table table-sm table-ssi">
                <thead><tr><th></th><th>Zone Type</th><th>Description</th><th>Details</th></tr></thead>
                <tbody>
                @foreach (var zone in Model.CurrentZones)
                {
                    <tr>
                        <td>
                            @if (zone.ZonePermissionId > 0)
                            {
                                <button type="submit" class="btn btn-link btn-sm p-0"
                                        formaction="?handler=RemoveZone&amp;zonePermissionId=@zone.ZonePermissionId"
                                        onclick="return confirm('Are you sure you want to delete this permission?');">
                                    Remove
                                </button>
                            }
                        </td>
                        <td>@zone.ZoneTypeName</td>
                        <td>@zone.Description</td>
                        <td>@zone.PrimaryFieldName: @zone.PrimaryIdValue</td>
                    </tr>
                }
                </tbody>
            </table>
        }

        <h2 class="h6 page-title mt-4">Add Zones</h2>
        <div class="fieldset-ssi">
            <div class="input-group input-group-sm mb-2">
                <input class="form-control" asp-for="ZoneSearchText" placeholder="Search zones" />
                <button class="btn btn-outline-secondary" type="submit" formaction="?handler=SearchZones">Search</button>
            </div>
            @if (Model.FoundZones.Count > 0)
            {
                <table class="table table-sm table-ssi">
                    <thead><tr><th></th><th>Type</th><th>Description</th></tr></thead>
                    <tbody>
                    @foreach (var zone in Model.FoundZones)
                    {
                        <tr>
                            <td><input type="checkbox" name="SelectedZoneIds" value="@zone.ZoneId" /></td>
                            <td>@zone.ZoneTypeName</td>
                            <td>@zone.Description</td>
                        </tr>
                    }
                    </tbody>
                </table>
            }
        </div>

        <div class="d-flex justify-content-between mt-3">
            <a class="btn btn-outline-secondary" asp-page="/Users/Index">&lt;&lt; Users</a>
            <button type="submit" class="btn btn-ssi">Save</button>
        </div>
    </form>
}
